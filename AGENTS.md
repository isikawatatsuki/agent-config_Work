# Agent設定集の共通ルール

- ユーザーへの応答、説明、文書は日本語にしてください。コード・識別子・コマンド・パスは原文を維持してください。
- 調査前に `REPOSITORIES.md` で対象を絞り、対象のAGENTS.md・README・仕様書を必要な範囲だけ確認してください。
- スキルは `.agents/skills/` に置き、追加・更新は同ディレクトリのREADMEに従ってください。日本語化、出典、ライセンス、安全規則を保持してください。
- 秘密情報、業務データ、生成物、個人設定をこの設定集へ含めないでください。
- 実装では既存の設計とテストを優先し、無関係な変更、既存変更の取り消し、依存の自動導入を行わないでください。
- 全依頼の作業記録は `docs/adr/README.md` に従ってください。実装・修正時は `implementation-handoff` を使い、同じ記録へ引き継ぎ事項を残してください。
- PR/MRの作成・本文更新・コメント投稿は、対象と下書きを提示し、その内容への明示承認を得るまで行わないでください。commit・push・mergeも明示依頼なしでは行わないでください。
- HTML/PDF成果物は明示指定がなければ `.agents/artifacts/<topic>/<YYYY-MM-DD-slug>/` に保存し、既存資料を移動・上書きしないでください。
- 設定例の配置だけでフック・タスク・スキルが有効になったと判断せず、利用先での認識と必要な依存を確認してください。
- `adrs` MCPはClaude Code専用です。Copilotでは登録・利用せず、CLIとMarkdownを使ってください。